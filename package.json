{
	"type": "module",
	"scripts": {
		"prod": "docker compose build && docker compose up -d",
		"test": "docker compose -p leaderboard-test -f docker-compose.test.yml build && docker compose -p leaderboard-test -f docker-compose.test.yml up -d"
	},
	"dependencies": {
		"express": "^5.2.1",
		"jsonwebtoken": "^9.0.3",
		"node-fetch": "^3.3.2",
		"postgres": "^3.4.7"
	}
}
